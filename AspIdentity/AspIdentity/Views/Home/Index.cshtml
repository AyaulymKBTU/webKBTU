@model IEnumerable<AspIdentity.Projects>
@*<div class="row">*@
   
    <div class="col-md-9">
        <div class="row">

            <table class="table">
                <tr>
                    <th>
                        PROJECTS
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Title)
                        </td>

                    </tr>
                }
            </table>
        </div>
   

</div>
